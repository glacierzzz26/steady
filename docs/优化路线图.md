# 优化路线图

> 依据当前代码现状核对（2026-08-20）后整理；「数据源稳定化」项已于 2026-08-23 复核为 ✅（代码已实现，见下）。已实现的能力标记 ✅，待办标记 🚧。

## 当前阶段判断

项目已过了"搭框架"阶段。可观测通知、数据与交易可靠性两条主线已按 2026-08-23 复核全部落地，当前工作重心应落在：

> ✅ 可观测通知（已完） → ✅ 数据与交易可靠性（已完） → **回测可信度校准** → 策略与风控迭代

## 数据源稳定化（Tushare 主源 + akshare 兜底）✅ 已完成

> 2026-08-23 复核：代码层面已全部落地（commit 8fb3fa9），此项从「部署后第一项」转为已完成。下面是当初规划与实现现状的逐条对照。

| 规划 | 现状 |
|---|---|
| 日线 / 估值 / 财务 Tushare 主源 → AkShare 兜底 | ✅ 全部接入：日线 `daily`+`adj_factor`、估值 `daily_basic`、财务 `fina_indicator`、股票列表 `stock_basic`、日历 `trade_cal`、指数 `index_daily`（`collector/app/sources/tushare.py`） |
| 每日增量主路径 | ✅ 按交易日全市场快照：日线 2 次调用/天、估值 1 次/天（`collector/app/tasks.py:75,130`） |
| 失败自动降级、不中断 | ✅ 接口异常 → 降级 AkShare，日志 `Tushare 失败(%s)，降级 AkShare`（`collectors/daily.py:151`、`finance.py:113`） |
| token 配置 | ✅ **实现为 `app_config.tushare.token`（前端设置页可改、存库、不读环境变量）**；`.env.example`/`config.yaml` 已无 `TUSHARE_TOKEN` 占位（比原规划的 env 方式更贴近「业务配置全走页面」原则） |
| 盘中实时快照保持 AkShare | ✅ 未动（热点采集纯 AkShare；Tushare 实时需积分，维持现状） |
| 15-min 历史回测 | ⏸ 暂不做（免费源深度不够，待冲积分再议） |

**验收标准对照**：
1. ✅ Tushare 主源日线同步成功（每日快照路径已在生产运行）
2. ✅ 失败自动降级、任务不失败（异常降级链路有实现）
3. ⚠️ 降级日志为 `降级 AkShare` 字样（非原定的 `source=akshare` 格式，但已可观测）

> 遗留：财务接口需 2000+ 积分，免费 120 积分档下财务实际走 AkShare；代码在 `fina_indicator` 首请求失败时快速整体降级，避免对全市场空转（`finance.py:105-113`）。

## 优先级一：完成飞书通知（Issue #5）✅ 已完成（2026-08-23 复核）

### 阶段一：统一通知基础设施

新增轻量通知模块：

```text
quant-engine/app/notify.py
backend/pkg/notify/notify.go
```

支持：

- `FEISHU_NOTIFY_ENABLED`
- `FEISHU_WEBHOOK_URL`
- 请求超时
- 有限重试
- 推送失败只记录日志，不阻塞主业务
- Webhook 地址脱敏日志

不单独拆通知微服务，保持现有架构（模块间仍通过 DB/定时任务协作）。

### 阶段二：接入关键任务

1. **策略信号生成**
   - 交易日期
   - 买入/卖出/持有数量
   - Top N 信号
   - 评分和原因摘要

2. **自动交易**（backend `ExecuteDay` 的 `ExecResult` 已含买/卖/拒单计数，直接透传）
   - 买入、卖出、拒绝数量
   - 幂等跳过原因
   - 失败订单摘要

3. **净值快照**（backend `NavResult` 已含 nav，补齐累计收益/回撤）
   - 当日净值
   - 累计收益
   - 持仓市值
   - 与基准收益对比

4. **回测完成**（`backtest_result` 已含指标）
   - 总收益
   - 年化收益
   - 最大回撤
   - Sharpe

### 阶段三：补齐日报 ✅

已实现：日报由 `quant-engine/app/notify_scheduler.py` 在 21:00 事件统一生成并推送（`tasks.py:5` 注释确认），内容汇总信号 / 交易 / 净值（`_daily_report_content`，`notify_scheduler.py:155`）。

> 实现与规划的两处差异（如实记录）：
> - 通知模块实际在 `quant-engine/app/notify.py` + `backend/internal/service/notify.go`（规划里写的 `backend/pkg/notify/notify.go` 是未采用的路径）。
> - 配置从环境变量改为 `app_config.feishu.*`（页面设置可改、存库、不读环境变量），`init.sql:380-388` 种子全 8 个事件。

## 优先级二：数据与交易可靠性

### 数据质量检查 ✅ 已完成（2026-08-23 复核）

实现：`quant-engine/app/data_quality.py`，18:30 由 `job_data_quality` 触发（`tasks.py:209`），7 项检查全落地（覆盖率 / 缺失日 / 重复 / 价格异常 / 估值滞后 / 财务新鲜度 / 指数基准，`data_quality.py:62-252`），结果写 `task_run` 台账（`tasks.py:178-189`），18:35 推飞书（notify_config `data_quality` 事件）。

### 交易一致性校验 ✅ 已完成（2026-08-23 复核）

实现：`backend/internal/service/consistency.go`，21:15 由 `sched.RegisterCatchUp("consistency-check", 21, 15, ...)` 触发（`main.go:84`），对「现金+市值=总资产、订单=成交、T+1 可卖数、净值幂等、自动交易不重复执行」等做独立复算，结果写 `task_run` 台账，对账通过/未通过均推飞书卡片。

## 优先级三：回测可信度校准

### 现状（规则已全部实现 ✅，无需从零补）

- 分板块涨跌停（主板 10% / ST 5% / 创业板 20% / 科创板 20% / 北交所 30%）— `broker.py`
- 印花税（万5，仅卖出）、佣金（万2.5、最低 5 元）、滑点 0.1% — `broker.py`
- T+1 冻结/解冻、100 股整手、加权平均成本 — `portfolio.py` + `engine.py`
- 停牌/数据缺失跳过、资金不足逐档减 100 股重试（与 Go 模拟盘一致）
- 沪深300 基准对比 + 超额收益 — `engine._get_benchmark_nav`
- 一致性对照测试已存在：`backend/internal/service/trading_consistency_test.go`、`quant-engine/tests/test_broker_consistency.py`

### 真正要做的 🚧

1. **防未来函数（look-ahead bias）验证** ⭐ 最高优先
   - 风险点：`engine.py` 中信号 T 日生成、**同一日**以 T 日收盘成交（`strategy.run(date)` → `_get_price(signal.code, date)`）。
   - 因子层已用 `announce_date <= trade_date`（财务）与 as-of 窗口（估值）防了部分未来函数 ✅，但行情类因子（MA/MACD）是"收盘算信号、收盘成交"，存在 look-ahead 风险。
   - 验证方案：对比 T 日收盘生成 + T 日成交 vs T+1 成交 两种方式的净值差异；确认是否需要改为 T+1 成交。
2. **信号生成日与成交日错位检查**：信号是 T 日盘中生成还是收盘后生成，成交价取哪一天，需明确并保持一致。
3. **固定 fixture 测试**（覆盖边界场景）：
   - 单只股票
   - 多只股票轮动
   - 涨停无法买入
   - 跌停无法卖出
   - 现金不足
   - T+1 卖出限制
   - 空数据区间
   - 停牌期间持有
4. **回测结果可复现性**：同一参数重复运行，结果一致。

## 优先级四：策略与风控迭代

### 现状 ✅（底层已具备）

- 因子权重存于 `factor_definition` 表，`backtest_service.py` 从 DB 读取——**权重已配置化**
- 策略参数（top_n / max_position_pct）存于 `strategy.params`，`execStrategy` 运行时解析

### 待补齐 🚧

- 因子权重 / 策略参数的**前端可视化配置界面**（当前只能改库）
- 多策略对比（同一区间跑多个策略，并排展示）
- 回测结果对比（历史回测任务横向比较）
- 换手率和交易成本分析
- 行业集中度限制
- 最大持仓比例风控
- 止损/止盈规则

暂不优先加入 AI 或机器学习，避免无法区分模型、数据和交易模拟问题。

## 推荐迭代顺序

```text
Iteration 0：部署上线 + 数据源稳定化
  ├── compose 稳定化（restart/内存限额/端口收敛/备份压缩）✅ 已提交
  ├── 数据源切换（Tushare 主源 + akshare 兜底）✅ 已完成（2026-08-23 复核）
  └── 异地备份（备份拷出 VM 系统盘）🚧 唯一未完成项

Iteration 1：完成 Issue #5
  ├── 飞书通知模块（Python + Go）✅ 已完成
  ├── 信号/交易/净值/回测推送 ✅ 已完成
  └── 实现真正的每日汇总日报 ✅ 已完成（notify_scheduler 21:00）

Iteration 2：可靠性与告警
  ├── 数据质量检查（记录 + 推送）✅ 已完成
  ├── 交易一致性对账校验 ✅ 已完成
  ├── 任务失败告警 ✅ 已完成（task_alert 红色告警）
  └── 数据库备份恢复验证 ✅ 已完成（2026-08-23 演练：从 192.168.0.201 拷今晨 02:30 备份到本机，恢复零错误，21 表行数对账通过；详见下次归档）

Iteration 3：回测可信度校准
  ├── 防未来函数验证（T 日 vs T+1 成交对比）⭐
  ├── 固定 fixture 边界测试
  ├── 回测与模拟盘规则一致性保持（已有一致性对照测试）
  └── 结果可复现性

Iteration 4：策略与风控
  ├── 多策略对比
  ├── 权重/参数前端配置
  ├── 仓位与行业风控
  └── 风险指标
```

## 最终目标

将系统从：

> 功能已经存在的演示平台

提升为：

> 每天能自动运行、产出结果、主动通知，并且结果可追溯、回测无未来函数偏差的量化研究平台。
