# 第二阶段（frontend-v2 重写 + 回测校准）

> **进行中。** 本阶段文档随工作推进持续更新，定稿的设计才归档到 `design/`，完成的子阶段才归档到 `stages/`。
> 起点：2026-08-23（frontend-v2 目录创建）。

## 范围

- **前端重写**：`frontend-v2/`（React 18 + TS + Vite + ECharts，当前纯 mock），后端适配 + 前端接真实 API；
- **回测可信度校准**（路线图 Iteration 3）：防未来函数验证（T 日 vs T+1 成交对比）、固定 fixture 边界测试、结果可复现性；
- 后续：Iteration 4 策略与风控（多策略对比、权重前端配置、风控）。

## 子目录约定

| 子目录 | 内容 |
|---|---|
| [`design/`](design/) | 本阶段**定稿**的设计（如 frontend-v2 接口契约）——定稿后才归档，未定稿的写活文档 |
| [`stages/`](stages/) | 本阶段完成的子阶段归档（复用 `../phase1/stages/_TEMPLATE.md` 模板） |

## 进行中的活文档

（随工作推进登记）前端 v2 接口契约草稿、回测校准实验记录。

## 归档触发

- 一个子阶段完成（如"后端适配 frontend-v2"）→ 写 `stages/<子阶段>.md`，登记到 `../进度总表.md`；
- 一项设计定稿（如接口契约）→ 写 `design/<主题>.md`，登记 `design/README.md`。
