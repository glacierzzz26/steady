# 第一阶段（frontend-v2 之前）

> **已归档完成。** 本阶段全部开发已结束，文档冻结不再改动（如确需修订，在 `进度总表.md` 登记后改）。
> 时间：2026-08-19 ~ 2026-08-22。

## 范围

搭框架 → 部署/数据源 → 飞书通知 → 可靠性与对账 → LLM 集成，即 frontend-v2 之前的所有开发。

| 子目录 | 内容 |
|---|---|
| [`design/`](design/) | 定稿设计文档（设计思路、技术准备） |
| [`stages/`](stages/) | 阶段归档（搭框架 / iter0 / iter1 / iter2 / llm），模板见 `stages/_TEMPLATE.md` |

## 上一阶段 → 下一阶段

本阶段结束时的系统：单机 docker-compose、每日自动"采集→因子→信号→模拟交易→净值对账→飞书通知"，已接入 LLM 简报解读，生产基线 Release `c8d0651` / `2de887a` / `d886a4d` / `3d08aa2` 全部合入 master。

**第二阶段的起点**：前端重写（frontend-v2）+ 回测可信度校准，见 [`../phase2/`](../phase2/README.md)。
