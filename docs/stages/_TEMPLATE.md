# <阶段名>

> 归档模板：每个阶段一页，复制本文件命名为 `<阶段名>.md`，按五节填写。
> 登记后更新 `../进度总表.md` 总表对应行。

## 目标

本阶段要解决什么问题、交付什么。一两段话，讲清"为什么做"。

## 时间

| 起 | 止 | 说明 |
|---|---|---|
| 2026-MM-DD | 2026-MM-DD | 按 commit 日期（`git log`） |

## 设计

本阶段的关键设计决策与取舍（重点写**决策**与**理由**，而不是罗列代码）。
定稿设计原文在 `../design/` 的，这里只给链接 + 一页浓缩；没有独立设计文档的，在这里写清要点。
设计变更/未采用方案也在此记录（如"原本打算 X，实际 Y"）。

## 实现

关键改动 + 对应提交（`git log --oneline` 摘录）。按主题分组，提交用短 SHA 关联。

## 验收

- ✅ 验收项与结果（能跑起来的：给出验证方式；能查的：给出代码位置 `文件:行号`）
- ⚠️ 部分达标/降级实现（如实记录与设计的差异）

## 遗留

本阶段留下的未决问题 / 下阶段接手的 TODO。**空的就写"无"**，不硬凑。
