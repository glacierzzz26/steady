# ===== 开发模式环境变量（host 直跑覆盖）=====
# 数据库：本机 Docker 容器（deploy/docker-compose.yml），代码服务全部 host 直跑
# backend config.yaml 默认 host=postgres（容器服务名），host 跑必须覆盖为 127.0.0.1
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=quant
DB_PASSWORD=quant_pass_2026
DB_NAME=quant_system
TZ=Asia/Shanghai
LOG_LEVEL=info
